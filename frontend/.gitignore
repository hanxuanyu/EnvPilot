# ============================================================
# EnvPilot frontend/.gitignore
# ============================================================

# ── Node.js 依赖 ─────────────────────────────────────────────
node_modules/

# ── Vite 构建产物 ─────────────────────────────────────────────
dist/
dist-ssr/

# ── TypeScript 编译缓存 ───────────────────────────────────────
*.tsbuildinfo

# ── 本地开发环境变量 ──────────────────────────────────────────
.env.local
.env.*.local

# ── 编辑器 ───────────────────────────────────────────────────
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── 日志 ─────────────────────────────────────────────────────
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── Wails 自动生成的 JS 绑定（保留在版本控制中）──────────────
# wailsjs/ 目录由 wails dev 自动生成，但提交到 git 以便：
#   1. CI/CD 环境无需运行 wails dev 即可执行 tsc 类型检查
#   2. 团队成员可以直接看到后端 API 的类型签名
# 注意：每次 wails dev 运行后，这些文件可能被更新，需重新提交
# wailsjs/
